version: '3.8'

services:
  redis:
    image: redis:latest
    container_name: mi-redis-compose
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    environment:
      - REDIS_PASSWORD=12345678

volumes:
  redis_data:
